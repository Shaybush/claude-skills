# {{Area}} Conventions

<!-- Root KB (~/.claude/docs-claude/kb/) = true in a repo that does not exist yet.
     Project KB (<repo>/docs-claude/kb/) = names a module/service/collection in one repo.
     Delete this comment once the file has content. -->

One or two sentences on what this file governs and what it deliberately leaves out.

## Structure

Where things live, and the rule for deciding placement when it is ambiguous.

## Patterns

The concrete, copyable shape of the common cases. Prefer a short real example over prose
describing one.

## Gotchas

Non-obvious behaviour that costs time to rediscover. State the reason **only** where the
reason stops someone "fixing" the rule back; otherwise state the rule alone.

## The gate

The exact commands that must be green before the work is done, and what each catches.

---

Keep entries dense — every line costs context on each read. Update this file when something
durable is learned; do not merely report it and move on.
