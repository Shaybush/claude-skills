# Cloudflare credentials — copy to .env (same folder) and fill in. .env is gitignored.
# Account ID: dashboard → account home → right sidebar (or GET /accounts).
CF_ACCOUNT_ID=''
# Scoped API token: dashboard → My Profile → API Tokens → Create Token.
CF_API_TOKEN=''
